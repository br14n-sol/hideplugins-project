groups:
  default:
    options:
      default: true
    global:
      commands: /about
      tab: /about
    worlds:
      world:
        commands: /help, /tell
        tab: /help, /tell
      world_nether:
        commands: /msg
        tab: /msg